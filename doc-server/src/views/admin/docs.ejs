<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document Manager - Guru Maa Admin</title>
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --bg-color: #f8fafc;
      --card-bg: #ffffff;
      --primary: #6366f1;
      --primary-soft: #eef2ff;
      --text-dark: #1e293b;
      --text-light: #64748b;
      --danger: #ef4444;
      --success: #10b981;
      --radius: 16px;
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background: var(--bg-color);
      color: var(--text-dark);
      font-family: 'Plus Jakarta Sans', sans-serif;
      line-height: 1.5;
    }

    .admin-layout {
      display: flex;
      min-height: 100vh;
    }

    /* Main Content Area */
    .main-content {
      flex: 1;
      margin-left: 280px; /* Aligned with our modern sidebar */
      padding: 40px;
      transition: all 0.3s ease;
    }

    /* Page Header */
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 40px;
      gap: 20px;
    }

    .header-info h1 {
      font-size: 32px;
      font-weight: 700;
      letter-spacing: -1px;
      color: var(--text-dark);
    }

    .header-info p {
      color: var(--text-light);
      margin-top: 4px;
    }

    /* Modern Upload Section */
    .upload-zone {
      background: var(--card-bg);
      border: 2px dashed #e2e8f0;
      border-radius: var(--radius);
      padding: 40px;
      text-align: center;
      transition: all 0.2s ease;
      margin-bottom: 40px;
      position: relative;
    }

    .upload-zone:hover {
      border-color: var(--primary);
      background: var(--primary-soft);
    }

    .upload-form {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 15px;
    }

    .file-input-wrapper {
      position: relative;
      cursor: pointer;
    }

    .upload-form input[type="file"] {
      position: absolute;
      left: 0; top: 0; opacity: 0;
      width: 100%; height: 100%;
      cursor: pointer;
    }

    .btn-upload-trigger {
      background: var(--primary);
      color: white;
      padding: 12px 30px;
      border-radius: 12px;
      font-weight: 600;
      border: none;
      cursor: pointer;
      box-shadow: 0 10px 15px -3px rgba(99, 102, 241, 0.3);
    }

    /* Document Grid (The Unique Part) */
    .doc-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 24px;
    }

    .doc-card {
      background: var(--card-bg);
      border-radius: var(--radius);
      padding: 24px;
      box-shadow: var(--shadow);
      border: 1px solid #f1f5f9;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .doc-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.05);
    }

    .doc-icon {
      width: 48px;
      height: 48px;
      background: var(--primary-soft);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      margin-bottom: 16px;
    }

    .doc-title {
      font-weight: 700;
      font-size: 16px;
      margin-bottom: 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .doc-meta {
      font-size: 13px;
      color: var(--text-light);
      margin-bottom: 20px;
    }

    .doc-meta span {
      display: inline-block;
      margin-right: 10px;
    }

    .doc-actions {
      display: flex;
      gap: 10px;
      border-top: 1px solid #f1f5f9;
      padding-top: 16px;
    }

    .btn-action {
      flex: 1;
      padding: 10px;
      border-radius: 8px;
      text-align: center;
      text-decoration: none;
      font-size: 13px;
      font-weight: 600;
      transition: 0.2s;
    }

    .btn-view { background: var(--primary-soft); color: var(--primary); }
    .btn-view:hover { background: var(--primary); color: white; }

    .btn-delete-icon { background: #fee2e2; color: var(--danger); border: none; cursor: pointer; }
    .btn-delete-icon:hover { background: var(--danger); color: white; }

    /* Empty State */
    .empty-container {
      grid-column: 1 / -1;
      text-align: center;
      padding: 60px;
      background: white;
      border-radius: var(--radius);
    }

    /* Responsive Adjustments */
    @media (max-width: 1024px) {
      .main-content { margin-left: 0; padding-top: 80px; }
      .page-header { flex-direction: column; align-items: flex-start; }
    }

    @media (max-width: 480px) {
      .doc-grid { grid-template-columns: 1fr; }
      .header-info h1 { font-size: 24px; }
    }
  </style>
</head>

<body>
  <div class="admin-layout">
    <%- include('./layouts/sidebar', { currentPage: 'docs' }) %>

    <main class="main-content">
      <header class="page-header">
        <div class="header-info">
          <h1>Asset Library</h1>
          <p>Organize and manage your digital documents</p>
        </div>
      </header>

      <section class="upload-zone">
        <form method="post" action="/admin/docs/upload" enctype="multipart/form-data" class="upload-form">
          <div class="doc-icon" style="margin: 0 auto 15px auto;">📁</div>
          <h3>Drop your files here</h3>
          <p style="color: var(--text-light); font-size: 14px; margin-bottom: 20px;">Support for PDF, DOCX, and Images</p>
          
          <div class="file-input-wrapper">
            <button type="button" class="btn-upload-trigger">Select File</button>
            <input type="file" name="file" required onchange="this.form.submit()" />
          </div>
        </form>
      </section>

      <section class="doc-grid">
        <% if (docs && docs.length > 0) { %>
          <% docs.forEach(function(doc) { %>
            <article class="doc-card">
              <div>
                <div class="doc-icon">
                  <% if(doc.type && doc.type.includes('pdf')) { %> 📕 
                  <% } else if(doc.type && doc.type.includes('image')) { %> 🖼️
                  <% } else { %> 📄 <% } %>
                </div>
                <h3 class="doc-title"><%= doc.title %></h3>
                <div class="doc-meta">
                  <span>📂 <%= doc.folder || 'Main' %></span>
                  <span>•</span>
                  <span>📅 <%= doc.type || 'File' %></span>
                </div>
              </div>

              <div class="doc-actions">
                <a href="<%= doc.url %>" target="_blank" class="btn-action btn-view">View Asset</a>
                
                <form method="post" action="/admin/docs/delete" style="flex: 0 0 45px;">
                  <input type="hidden" name="id" value="<%= doc.id %>" />
                  <button type="submit" class="btn-action btn-delete-icon" onclick="return confirm('Delete permanently?')">
                    🗑️
                  </button>
                </form>
              </div>
            </article>
          <% }) %>
        <% } else { %>
          <div class="empty-container">
            <div style="font-size: 50px; margin-bottom: 20px;">☁️</div>
            <h3>Your library is empty</h3>
            <p style="color: var(--text-light);">Start by uploading a document above.</p>
          </div>
        <% } %>
      </section>

      <div style="margin-top: 40px;">
        <%- include('./layouts/pagination') %>
      </div>
    </main>
  </div>

  <%- include('./layouts/flash-toast') %>

  <script>
    // Smooth file name feedback (Optional)
    const fileInput = document.querySelector('input[type="file"]');
    fileInput.addEventListener('change', function(e) {
      if(this.files.length > 0) {
        document.querySelector('.upload-zone h3').innerText = "Uploading: " + this.files[0].name;
      }
    });
  </script>
</body>
</html>