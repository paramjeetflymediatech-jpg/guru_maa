new updated


server {
  listen 80;
  listen [::]:80;
  listen 443 ssl;
  listen [::]:443 ssl;

  http2 on;

  ssl_certificate_key /etc/nginx/ssl-certificates/theenglishraj.com.key;
  ssl_certificate /etc/nginx/ssl-certificates/theenglishraj.com.crt;

  server_name theenglishraj.com www1.theenglishraj.com;
  root /home/theenglishraj/htdocs/theenglishraj.com;
  index index.html;

  access_log /home/theenglishraj/logs/nginx/access.log main;
  error_log /home/theenglishraj/logs/nginx/error.log;

  # 🔑 REACT SPA FIX
  location / {
      try_files $uri $uri/ /index.html;
  }

  location ~ /.well-known {
    auth_basic off;
    allow all;
  }

  location ~* ^.+\.(css|js|jpg|jpeg|gif|png|ico|gz|svg|ttf|woff|woff2|eot|mp4|webm|zip)$ {
    add_header Access-Control-Allow-Origin "*";
    expires max;
    access_log off;
  }

  include /etc/nginx/global_settings;
}
